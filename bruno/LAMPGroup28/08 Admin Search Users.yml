info:
  name: Admin Search Users
  type: http
  seq: 8

http:
  method: POST
  url: http://167.99.2.103/API/AdminSearchUsers.php
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer 1
  body:
    type: json
    data: |-
      {
        "search": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  Must verify that the caller is an active Admin before returning users.
