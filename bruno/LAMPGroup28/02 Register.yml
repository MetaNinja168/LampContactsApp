info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: http://167.99.2.103/API/Register.php
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "firstName": "Test",
        "lastName": "User",
        "login": "testuser",
        "password": "ChangeMe123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  Registers a normal user. The PHP endpoint must hash the password before storing it.
