info:
  name: Set User Status
  type: http
  seq: 10

http:
  method: POST
  url: http://167.99.2.103/API/SetUserStatus.php
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer 1
  body:
    type: json
    data: |-
      {
        "userId": 2,
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  Disables or re-enables an account without deleting it. Must verify the caller is an active Admin.
