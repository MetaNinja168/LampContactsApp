info:
  name: Search Contacts
  type: http
  seq: 5

http:
  method: GET
  url: http://167.99.2.103/API/SearchContacts.php?q=Jamie
  headers:
    - name: Authorization
      value: Bearer 1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  The PHP endpoint must execute the search in MySQL instead of loading every contact into the browser.
